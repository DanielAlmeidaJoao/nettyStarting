registerMessageHandler nao tem que precisar do ID do canal, tem que
haver um mapa por protocolo com os handlers
        //TODO VERIFY IF THE PARAMETERS OF THE HANDLERS ARE THE RIGHT TYPE

find an annotation that removes the "never used warning on methods that are using the custom interface"

 mvn clean compile package install