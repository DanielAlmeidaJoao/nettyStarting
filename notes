java -Xmx500M -cp asdProj2.jar Main p2p_port=10101 server_port=35000 &
java -cp target\nettyStart.jar Main

java -cp target\nettyStart.jar org.streamingAPI.connectionSetups.StreamSender localhost 8081
java -cp target\nettyStart.jar org.streamingAPI.connectionSetups.StreamInConnection 8081

java -cp target/nettyStart.jar org.streamingAPI.connectionSetups.StreamSender localhost 8081
java -cp target/nettyStart.jar org.streamingAPI.fileStreamingApp.FileStreamer localhost 8081

java -cp target/nettyStart.jar org.streamingAPI.connectionSetups.StreamInConnection 8081

java -cp target/nettyStart.jar org.streamingAPI.fileStreamingApp.FileReceiver 8081

java -cp target/nettyStart.jar org.streamingAPI.p2p.PeerToPeerChannel

WINDOWS:
java -cp target/nettyStart.jar org.streamingAPI.fileStreamingApp.FileReceiver 8081

java -cp target/nettyStart.jar org.streamingAPI.fileStreamingApp.FileStreamer localhost 8081



the sender must be the server
the receiver must be the client

Starting Babel

java -Xmx500M -cp target/nettyStart.jar Main sender=o

java -cp target/nettyStart.jar Main address=localhost port=10101

java -cp target/nettyStart.jar Main address=localhost port=10103 sender=o

java -cp target/nettyStart.jar Main address=localhost port=10109 sender=o forwarder=f

QUIC
java -cp target/nettyStart.jar quicSupport.client_server.QuicServerExample
java -cp target/nettyStart.jar quicSupport.client_server.QuicClientExample

java -cp target/nettyStart.jar quicSupport.testing.TestQuicChannel
-Dio.netty.maxDirectMemory=2147483648

java -Dio.netty.maxDirectMemory=4147483648 -cp target/nettyStart.jar Main2 8081

java java -Xmx3g -cp target/nettyStart.jar Main2 8082


Udp
java -cp target/nettyStart.jar udpSupport.client.UdpClient
java -cp target/nettyStart.jar udpSupport.server.UdpServer

DOUBTS:
QUIC CONNECTION DISCONNECT THE OTHER PEER DOES NOT KNOW
SEND MESSAGE TO DISONNECTED PEER DOES NO FAIL (DOES NOT TRIGGER ERROR)

PROCURAR OUTRAS IMPLEMENTACOES DO QUIC EM JAVA.
DO ANOTHER CHANNEL WHICH WILL USE ACKs, and HEARTBEATS (PERIOD: MAX_IDLE/2).